<?xml version="1.0" encoding="utf-8"?>
<androidx.preference.PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Experimental Settings from original settings.xml -->
    <androidx.preference.CheckBoxPreference
        android:key="enable_webgl"
        app:title="Enable WebGL"
        app:summary="Use hardware acceleration for image rendering (experimental)"
        app:defaultValue="false" />

    <androidx.preference.CheckBoxPreference
        android:key="enable_prefetch"
        app:title="Prefetch pages"
        app:summary="Preload next/previous pages for faster navigation (uses more bandwidth)"
        app:defaultValue="false" />

    <androidx.preference.CheckBoxPreference
        android:key="enable_experimental_parser"
        app:title="Experimental parser"
        app:summary="Use new parsing engine (may be unstable)"
        app:defaultValue="false" />

    <androidx.preference.CheckBoxPreference
        android:key="debug_mode"
        app:title="Debug mode"
        app:summary="Enable debug logging and additional information"
        app:defaultValue="false" />

</androidx.preference.PreferenceScreen>